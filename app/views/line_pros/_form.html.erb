<%= form_with(model: line_pro, local: true) do |form| %>
  <% if line_pro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_pro.errors.count, "error") %> prohibited this line_pro from being saved:</h2>

      <ul>
        <% line_pro.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :cart_id %>
    <%= form.number_field :cart_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
